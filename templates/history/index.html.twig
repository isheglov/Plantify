{% include 'header.html.twig' %}

{# menu buttons #}
<div class="nav-bar">
    {% include 'common/menu.html.twig' %}
</div>

<h1 class="title">История посадок</h1>

<div style="text-align: center;"><a href="/history/2021/3"><< Раньше || </a><span class="title"> {{ date }}</span><a href="/history/2021/4"> || Позже >></a></div>


<div>
    <h2 id="t">Схема участка</h2>
    <table>
        {% for i in 0..dimensionY-1 %}
            <tr>
                {% for j in 0..dimensionX-1 %}
                    <td id="cell{{ gardenCellList[j][i]['cellId'] }}">
                        <div class="cell-content{{ gardenCellList[j][i]['cellId'] }} cell-content">
                            <h1>{{ gardenCellList[j][i]['plantName'] }}</h1>
                        </div>
                    </td>
                {% endfor %}
            </tr>
        {% endfor %}
    </table>
</div>






{% for historyItem in historyList %}
    <div class="modal-item">
        {{ historyItem.name }}<b>С: {{ historyItem.dateFrom }}</b> {% if historyItem.dateTo != '' %}<b>По: {{ historyItem.dateTo }}</b>{% endif %}

        <a href="/planning/{{ historyItem.cell }}">На схеме</a>

        На грядке № {{ historyItem.cell }}
    </div>
{% endfor %}

{% include 'footer.html.twig' %}
